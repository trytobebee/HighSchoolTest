#include <iostream>
//#include <list>
using namespace std;
//#include "chessBoard.h"

int main()
{
    int n, k;
    cin >> n >> k;
    int *array = new int[n];

    for(int i = 0; i < n; i++)
    {
        cin >> array[i];
    }

    return 0;
}